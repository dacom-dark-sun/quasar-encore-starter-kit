{
  "name": "monocoop",
  "private": true,
  "dependencies": {
    "frontend": "workspace:*",
    "backend": "workspace:*"
  },
  "scripts": {
    "gen": "cd backend && encore gen client --output=../frontend/src/shared/api/client.ts --env=local"
  }
}
